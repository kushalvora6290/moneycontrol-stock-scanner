name: NIFTY 500 Intraday Scanner

on:
  schedule:
    - cron: "*/5 3-10 * * 1-5"   # 8:30 AM – 3:30 PM IST (safe window)
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install yfinance pandas requests pytz

      - name: Run NIFTY 500 scanner
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: python nifty500_intraday.py
