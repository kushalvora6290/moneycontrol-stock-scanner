name: Relative Strength Intraday Scanner

on:
  schedule:
    - cron: "*/5 3-10 * * 1-5"   # 8:30 AM – 3:30 PM IST (safe window)
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install yfinance pandas requests pytz

      - name: Run Relative Strength Scanner
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: python relative_strength_intraday.py
